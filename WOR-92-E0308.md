# WOR-92: Fix E0308 Type Mismatch Errors (7 remaining)

**Parent:** WOR-91
**Status:** todo
**Priority:** high

## Errors to Fix

### 1. `ElevationGrid` vs `TerrainGrid` (generation/mod.rs:166,169,175,192,194)
Methods expect `ElevationGrid` but receive `TerrainGrid`.

### 2. `Vec2<f32>` vs `Vec2<i32>` (hydro/drainage_basin.rs:60)
Use `Vec2::ZERO` with type parameter.

### 3. `BiomeType` vs `&BiomeType` (simulation/population.rs:635)
Method signature mismatch.

### 4. `EventType` dereferencing (events/probability/engine.rs:618,623)
Cannot dereference `EventType`.

## Suggested Fixes

1. **generation/mod.rs**: Cast `TerrainGrid` to `ElevationGrid` or update method signatures
2. **drainage_basin.rs**: `Vec2::<f32>::ZERO` or update type
3. **population.rs**: Pass `&biome` directly or adjust method
4. **engine.rs**: Remove `*` dereference on EventType